"use client";import React from"react";import{useBanking}from"@/features/banking/useBanking";export function ToastContainer(){const{toasts,removeToast}=useBanking();if(!toasts?.length)return null;return <div className="fixed bottom-6 right-6 z-50 flex flex-col gap-3 max-w-sm w-full px-4 font-mono">{toasts.map(t=><div key={t.id} className="border-[3px] border-[#0a0a0a] bg-[#e8ff00] p-4 shadow-hard-lg flex justify-between"><span className="text-[12px] font-extrabold uppercase">✓ {t.message}</span><button onClick={()=>removeToast(t.id)}>✕</button></div>)}</div>}
