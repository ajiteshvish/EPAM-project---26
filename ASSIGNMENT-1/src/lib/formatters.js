export function formatCurrency(amount,currency="GBP"){return new Intl.NumberFormat("en-GB",{style:"currency",currency,minimumFractionDigits:2,maximumFractionDigits:2}).format(typeof amount==="number"?amount:parseFloat(amount)||0)}export function formatTransactionDate(s){if(!s)return"";const d=new Date(s);if(isNaN(d.getTime()))return s;return d.toLocaleDateString("en-GB",{day:"2-digit",month:"short",year:"numeric"})}export function formatAccountNumber(n){if(!n)return"";const c=n.replace(/\D/g,"");return c.length<4?n:`•••• ${c.slice(-4)}`}export function formatSortCode(s){if(!s)return"";const c=s.replace(/\D/g,"");return c.length===6?`${c.slice(0,2)}-${c.slice(2,4)}-${c.slice(4,6)}`:s}
