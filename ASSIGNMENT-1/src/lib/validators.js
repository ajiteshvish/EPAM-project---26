export function validateTransferAmount(amount,balance){const n=parseFloat(amount);if(!amount||isNaN(n))return"Please enter a valid amount.";if(n<=0)return"Amount must be greater than £0.00.";if(n>balance)return`Insufficient funds. Your available balance is £${balance.toFixed(2)}.`;return null}export function validateAccountNumber(n){if(!n||typeof n!=="string")return"Account number is required.";return /^\d{8,16}$/.test(n.replace(/[\s-]/g,""))?null:"Invalid account number (must be 8-16 digits)."}export function validateSortCode(n){if(!n||typeof n!=="string")return"Sort code is required.";return /^\d{6}$/.test(n.replace(/[\s-]/g,""))?null:"Invalid sort code (must be 6 digits)."}export function validateNote(n,max=100){return n&&n.length>max?`Reference note cannot exceed ${max} characters.`:null}
